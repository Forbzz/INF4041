<form action="{{ path('EsieaBlogBundle_comment_create', { 'blog_id' : comment.blog.id } ) }}" method="post" {{ form_enctype(form) }} class="Esiea">
   {{ form_widget(form) }}
  <p>
    <input type="submit" value="Submit">
  </p>
</form>